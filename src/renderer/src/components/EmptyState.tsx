export function EmptyState() {
  return (
    <div className="centered empty-state">
      <p>No Claude Code usage found yet.</p>
      <p className="text-muted">Use Claude Code in a project, then hit Refresh.</p>
    </div>
  )
}
